{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); sys.exit(0 if p.endswith(('.ts','.tsx')) else 1)\" && cd /home/dhiraj/work/ICT-Awards && ./node_modules/.bin/tsc --noEmit 2>&1 | tail -20 || true",
            "statusMessage": "Type-checking..."
          }
        ]
      }
    ]
  }
}
